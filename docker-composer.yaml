version: '3.8'
services:
  web:
    container_name: raajeshsharma_github_io
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    command: pnpm run dev
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
